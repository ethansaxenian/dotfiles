#!/usr/bin/env zsh

# initializes a new git repository connected to a remote url

if [[ -z "$1" ]]; then
  echo "No remote url, aborting..."
  exit 1
fi

git init
git remote add origin "$1"
git add -A
git commit -m "initial commit"
git push -u origin main
